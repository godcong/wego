package mini

import (
	"testing"
)

func TestMpAccessToken_Session(t *testing.T) {
	//acc := wego.NewMiniProgramAccessToken(wego.GetApplication(), wego.GetConfig("mini.default"))
	//s := acc.Session("1234")
	//log.Println(s)
}
